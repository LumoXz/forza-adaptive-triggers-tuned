# 交接文档（Hermes → Z Code）

> 2026-08-28 移交。改这份代码前先读完本文档，尤其是"踩过的坑"一节——每条都是实测换来的。

## 项目是什么

Forza Horizon 5（Steam 版）+ PS5 DualSense 手柄的自适应扳机工具。读游戏 UDP 遥测（127.0.0.1:5300，~82Hz），直接写 DualSense HID 报告驱动扳机马达。基于 MIT 许可的 [Jason13201/forza-adaptive-triggers](https://github.com/Jason13201/forza-adaptive-triggers) fork，全部源码已过审（无网络外联/无落盘/无注册表）。

技术栈：**.NET 8 (net8.0-windows)、C#、HidSharp**。编译命令：

```
dotnet publish ForzaAdaptiveTriggers -c Release -r win-x64 --self-contained -p:PublishSingleFile=true
```

## 设计哲学（已定，别改回旧路）

**"GTA5 式真弹簧"**：扳机力度只跟随油门/刹车踏板值（Accel/Brake 0-255 映射到力度），高频遥测（RPM、速度、打滑、boost）**一律不进力度公式**。效果类"点睛"低频开关是以后再说的事，当前基线就是纯净弹簧。

右扳机（feel v6，用户确认"感觉不错"）参数：
- 曲线：`force = (Accel/255)^1.5 × 255`（渐进弹簧）
- 迟滞：进 60 / 出 10（防临界闪烁）
- 力度底：45（过了底力才有弱区，弱区最显抖）
- EMA：alpha 0.12（~110ms，滤掉游戏侧换挡/TC 调制）
- 量化：12/级（~5%，杀掉马达"逐帧追数字"的打枪感）

## 踩过的坑（按代价排序，勿重蹈）

1. **冻结检测必须保持禁用**（原版功能，已从源码删除）。"30 帧 accel/brake/speed 全同=暂停"会误杀小油门匀速巡航，周期性释放扳机 = 抖动。菜单场景 `IsRaceOn==0` 已覆盖，它是冗余的。
2. **"打枪感"根源是逐帧 ±1~3 力度的高频写入**，不是开关抖动。解法是量化步长 + 重 EMA（见上），别去动迟滞带。
3. **HapticsMapper 保持 `Enabled=false`**：游戏自带震动够用；且马达写入与扳机写入共享同一 HID 报告，会互相整包覆盖打架。
4. **蓝牙下扳机效果被 Windows BT HID 静默忽略**——只能 USB。README 已写明。
5. **Steam 侧 DualSense 震动要关**（属性→控制器→其他设置），否则 Steam 写 HID 报告和这个工具打架。README 已写明。
6. **调手感必须一次改一个方向**：第一版一次塞了太多遥测效果，用户连抖了六轮才定位真凶。

## 当前状态与待办

| 项 | 状态 |
|---|---|
| 右扳机 | ✅ feel v6（提交 721b433），用户确认满意 |
| 左扳机（刹车） | ⚠️ 还是线性 + 旧参数（alpha 0.25、无量化）——**下一步：套用右扳机配方**（曲线+重 EMA+量化），但刹车是否需要曲线由用户实测定，刹车侧可预期性优先 |
| 参数微调 | 均为 `Mapping/*.cs` 顶部常量，一处一行，编译约 40 秒 |
| 编译产物 | 不在仓库内，publish 后自行产出 |

## 验收方式（这个项目没有自动化测试，验收=上手感）

改完编译 → 用户 USB 连手柄 → 启动 exe → FH5 上路（遥测设置：HUD 和游戏性→数据输出→开，127.0.0.1:5300）→ 用户开车试手感反馈。**任何手感改动必须让用户实测后才能定论，不要凭代码推理宣称"会更好"。**

## 工作约定

- 提交信息沿用现有风格（conventional commits）
- 注释写"为什么"（现有代码全部如此，是用户明确的要求）
- 参数改动保持"一个常量一行注释解释作用域和取值依据"的格式
